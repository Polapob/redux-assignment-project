import reducer from "../../../features/auth/authSlice";
import { InitialAuthInterface } from "../../../features/auth/state";

describe("test auth slice", () => {
  it("check initial state", () => {});
  // expect(reducer(undefined, { type: undefined })));
});
